{
	"devDependencies": {
		"@playwright/test": "^1.59.1",
		"husky": "^9.1.7"
	},
	"scripts": {
		"prepare": "husky",
		"validate:gcode": "pnpm -C scripts validate:gcode",
		"validate:golden": "pnpm -C scripts validate:golden",
		"test:deterministic-export": "pnpm -C scripts test:deterministic-export"
	}
}
